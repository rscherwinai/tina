/** @type {import('next').NextConfig} */
const nextConfig = {
  speedInsights: {
    enabled: false,
  },
  devIndicators: {
    buildActivityPosition: 'bottom-right',
  },
  feedback: {
    enabled: false,
  },
}

module.exports = nextConfig 