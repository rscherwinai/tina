/** @type {import('next').NextConfig} */
const nextConfig = {
  speedInsights: {
    enabled: false,
  },
  devIndicators: {
    buildActivityPosition: 'bottom-right',
  },
}

module.exports = nextConfig 