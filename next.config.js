/** @type {import('next').NextConfig} */
const nextConfig = {
  
  devIndicators: {
    buildActivityPosition: 'bottom-right',
  },
  feedback: {
    enabled: false,
  },
}

module.exports = nextConfig 